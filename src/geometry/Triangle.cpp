#include "Triangle.h"
#include "../math/vector.h"

namespace Renderer
{
	void drawTriangle(Point& p1, Point& p2, Point& p3)
	{
		Vector2<int> v1(p1);
		Vector2<int> v2(p2);
		Vector2<int> v3(p3);
	}
}